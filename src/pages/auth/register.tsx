import AuthContent from "./auth";

const RegisterPage = () => {
  const handleRegister = () => {
    //TODO: to get from auth
  };

  return <AuthContent handleSubmit={handleRegister} isRegistration />;
};

export default RegisterPage;
