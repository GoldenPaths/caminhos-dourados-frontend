import AuthContent from "./auth";

const LoginPage = () => {
  const handleLogin = () => {
    //TODO: to get from auth
  };

  return <AuthContent handleSubmit={handleLogin} />;
};

export default LoginPage;
